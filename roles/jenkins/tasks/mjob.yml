- name: copy  configuration file
  ansible.builtin.template:
    src: mjob.xml
    dest: /tmp/mjob.xml

- name: Create a jenkins job
  community.general.jenkins_job:
    config: "{{ lookup('file', 'tmp/mjob.xml') }}"
    name: "{{ item.job_name}}"
    password: admin123
    url: http://52.86.6.252:8080
    user: admin